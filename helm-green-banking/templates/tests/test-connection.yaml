apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-green-banking.fullname" . }}-test-connection"
  labels:
    {{- include "helm-green-banking.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-green-banking.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
