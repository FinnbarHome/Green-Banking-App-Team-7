apiVersion: v1
kind: Service
metadata:
  name: {{ include "green-banking.fullname" . }}
  labels:
    {{- include "green-banking.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "green-banking.selectorLabels" . | nindent 4 }}
